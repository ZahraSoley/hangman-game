// import clsx from "clsx"

// export const HangmanWord = () => {
//     const word = 'test'
//     const guesssedLetters = ['t', 'e']

//     const letters = word.split('')
//     const takeLetter = (letter: string, index: number) => (
//         <span className="border-solid border-black border-b-6" key={index}>
//             <span className={clsx(guesssedLetters.includes(letter) ? 'visible' : 'invisible')}>{letter}</span>
//         </span>
//     )

//     return (
//         <div className="flex gap-5 text-5xl font-bold uppercase font-mono">
//             {letters.map(takeLetter)}
//         </div>

//     )
// }




const HangmanWord = () => {
    return (
        <div>HangmanWord</div>
    )
}

export default HangmanWord
